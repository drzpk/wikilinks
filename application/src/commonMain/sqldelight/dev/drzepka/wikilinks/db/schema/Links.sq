CREATE TABLE Links (
    page_id INTEGER NOT NULL PRIMARY KEY,
    in_links TEXT NOT NULL,
    out_links TEXT NOT NULL
);

insert:
INSERT INTO Links VALUES (?, ?, ?);

getOutLinks:
SELECT out_links FROM Links WHERE page_id = ?;
